-- [[ APPLE LIB: IMAGE FIX EDITION ]] --
local Library = {}
Library.__index = Library

local CoreGui = game:GetService("CoreGui")
local Players = game:GetService("Players")
local MarketplaceService = game:GetService("MarketplaceService")
local UIS = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")

local LocalPlayer = Players.LocalPlayer

-- Metadata
local success1, GameName = pcall(function() return MarketplaceService:GetProductInfo(game.PlaceId).Name end)
if not success1 then GameName = "Unknown Game" end

local success2, Headshot = pcall(function() return Players:GetUserThumbnailAsync(LocalPlayer.UserId, Enum.ThumbnailType.HeadShot, Enum.ThumbnailSize.Size100x100) end)
if not success2 then Headshot = "rbxassetid://0" end

-- Smarter Dragging
local function EnableCustomDrag(Frame)
    local Dragging, DragStart, StartPos
    Frame.InputBegan:Connect(function(input)
        if (input.UserInputType == Enum.UserInputType.MouseButton1) and not _G.ViperDraggingSlider then
            Dragging = true
            DragStart = input.Position
            StartPos = Frame.Position
            input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then Dragging = false end
            end)
        end
    end)
    UIS.InputChanged:Connect(function(input)
        if Dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
            local Delta = input.Position - DragStart
            Frame.Position = UDim2.new(StartPos.X.Scale, StartPos.X.Offset + Delta.X, StartPos.Y.Scale, StartPos.Y.Offset + Delta.Y)
        end
    end)
end

function Library.new(title)
    local self = setmetatable({}, Library)
    _G.ViperDraggingSlider = false
    self.Minimized = false
    
    local RawID = "18131589345" 
    local LogoID = "rbxassetid://" .. RawID:gsub("rbxassetid://", "")
    
    self.Gui = Instance.new("ScreenGui")
    self.Gui.Name = "AppleLib_Final"
    self.Gui.Parent = (gethui and gethui()) or CoreGui

    -- STARTUP OVERLAY
    local startupFrame = Instance.new("Frame")
    startupFrame.Size = UDim2.new(1, 0, 1, 0)
    startupFrame.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
    startupFrame.ZIndex = 100
    startupFrame.Parent = self.Gui

    local logo = Instance.new("ImageLabel")
    logo.Size = UDim2.new(0, 100, 0, 100)
    logo.Position = UDim2.new(0.5, -50, 0.45, -50)
    logo.Image = LogoID
    logo.BackgroundTransparency = 1
    logo.ZIndex = 101
    logo.Parent = startupFrame

    local loadBarBG = Instance.new("Frame")
    loadBarBG.Size = UDim2.new(0, 200, 0, 4)
    loadBarBG.Position = UDim2.new(0.5, -100, 0.62, 0)
    loadBarBG.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
    loadBarBG.BorderSizePixel = 0
    loadBarBG.ZIndex = 101
    loadBarBG.Parent = startupFrame
    Instance.new("UICorner", loadBarBG)

    local loadBarFill = Instance.new("Frame")
    loadBarFill.Size = UDim2.new(0, 0, 1, 0)
    loadBarFill.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    loadBarFill.ZIndex = 102
    loadBarFill.Parent = loadBarBG
    Instance.new("UICorner", loadBarFill)

    local welcomeText = Instance.new("TextLabel")
    welcomeText.Size = UDim2.new(1, 0, 0, 20)
    welcomeText.Position = UDim2.new(0, 0, 0, 15)
    welcomeText.Parent = loadBarBG
    welcomeText.BackgroundTransparency = 1
    welcomeText.Text = "Welcome to Apple Lib"
    welcomeText.TextColor3 = Color3.fromRGB(200, 200, 200)
    welcomeText.Font = Enum.Font.Gotham
    welcomeText.TextSize = 12
    welcomeText.ZIndex = 101
    welcomeText.TextTransparency = 1

    -- MAIN UI SETUP
    self.MainFrame = Instance.new("Frame")
    self.MainFrame.Size = UDim2.new(0, 550, 0, 400)
    self.MainFrame.Position = UDim2.new(0.5, -275, 0.55, -200)
    self.MainFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
    self.MainFrame.BackgroundTransparency = 0.3 
    self.MainFrame.ClipsDescendants = true
    self.MainFrame.Visible = false
    self.MainFrame.Parent = self.Gui

    EnableCustomDrag(self.MainFrame)
    Instance.new("UICorner", self.MainFrame).CornerRadius = UDim.new(0, 12)
    Instance.new("UIStroke", self.MainFrame).Color = Color3.fromRGB(255, 255, 255)

    self.Body = Instance.new("Frame")
    self.Body.Size = UDim2.new(1, 0, 1, -40)
    self.Body.Position = UDim2.new(0, 0, 0, 40)
    self.Body.BackgroundTransparency = 1
    self.Body.Parent = self.MainFrame

    local sidebarBG = Instance.new("Frame")
    sidebarBG.Size = UDim2.new(0, 160, 1, 0)
    sidebarBG.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
    sidebarBG.BackgroundTransparency = 0.6 
    sidebarBG.Parent = self.Body
    Instance.new("UICorner", sidebarBG).CornerRadius = UDim.new(0, 12)

    local topBar = Instance.new("Frame")
    topBar.Size = UDim2.new(1, 0, 0, 40)
    topBar.BackgroundTransparency = 1
    topBar.Parent = self.MainFrame

    local function createLight(color, xPos, callback)
        local light = Instance.new("TextButton")
        light.Size = UDim2.new(0, 12, 0, 12)
        light.Position = UDim2.new(0, xPos + 15, 0.5, -6)
        light.BackgroundColor3 = color
        light.Text = ""
        light.Parent = topBar
        Instance.new("UICorner", light).CornerRadius = UDim.new(1, 0)
        if callback then light.MouseButton1Click:Connect(callback) end
    end

    createLight(Color3.fromRGB(255, 95, 87), 0, function() self.Gui:Destroy() end)
    createLight(Color3.fromRGB(254, 188, 46), 22, function()
        self.Minimized = not self.Minimized
        local goalSize = self.Minimized and UDim2.new(0, 550, 0, 40) or UDim2.new(0, 550, 0, 400)
        TweenService:Create(self.MainFrame, TweenInfo.new(0.5, Enum.EasingStyle.Quart), {Size = goalSize}):Play()
        self.Body.Visible = not self.Minimized
    end)
    createLight(Color3.fromRGB(40, 200, 64), 44)

    -- Profile Section
    local profile = Instance.new("Frame")
    profile.Size = UDim2.new(0, 140, 0, 50)
    profile.Position = UDim2.new(0, 10, 1, -60)
    profile.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    profile.BackgroundTransparency = 0.94
    profile.Parent = self.Body
    Instance.new("UICorner", profile).CornerRadius = UDim.new(0, 8)

    local pfp = Instance.new("ImageLabel")
    pfp.Size = UDim2.new(0, 32, 0, 32)
    pfp.Position = UDim2.new(0, 8, 0.5, -16)
    pfp.Image = Headshot
    pfp.BackgroundTransparency = 1
    pfp.Parent = profile
    Instance.new("UICorner", pfp).CornerRadius = UDim.new(1, 0)

    local uName = Instance.new("TextLabel")
    uName.Size = UDim2.new(1, -50, 0, 18)
    uName.Position = UDim2.new(0, 48, 0, 8)
    uName.Text = LocalPlayer.DisplayName
    uName.TextColor3 = Color3.fromRGB(255, 255, 255)
    uName.Font = Enum.Font.GothamBold
    uName.TextSize = 11
    uName.BackgroundTransparency = 1
    uName.TextXAlignment = Enum.TextXAlignment.Left
    uName.Parent = profile

    local gName = Instance.new("TextLabel")
    gName.Size = UDim2.new(1, -50, 0, 18)
    gName.Position = UDim2.new(0, 48, 0, 22)
    gName.Text = GameName
    gName.TextColor3 = Color3.fromRGB(180, 180, 180)
    gName.Font = Enum.Font.Gotham
    gName.TextSize = 9
    gName.BackgroundTransparency = 1
    gName.TextXAlignment = Enum.TextXAlignment.Left
    gName.Parent = profile

    self.TabList = Instance.new("Frame")
    self.TabList.Size = UDim2.new(0, 160, 1, -110)
    self.TabList.Position = UDim2.new(0, 0, 0, 5)
    self.TabList.BackgroundTransparency = 1
    self.TabList.Parent = self.Body
    Instance.new("UIListLayout", self.TabList).Padding = UDim.new(0, 5)

    self.PageContainer = Instance.new("Frame")
    self.PageContainer.Size = UDim2.new(1, -180, 1, -20)
    self.PageContainer.Position = UDim2.new(0, 170, 0, 10)
    self.PageContainer.BackgroundTransparency = 1
    self.PageContainer.Parent = self.Body

    -- STARTUP ANIMATION
    task.spawn(function()
        task.wait(0.5)
        TweenService:Create(welcomeText, TweenInfo.new(1), {TextTransparency = 0}):Play()
        TweenService:Create(loadBarFill, TweenInfo.new(2.5, Enum.EasingStyle.Quart), {Size = UDim2.new(1, 0, 1, 0)}):Play()
        task.wait(2.7)
        TweenService:Create(startupFrame, TweenInfo.new(0.5), {BackgroundTransparency = 1}):Play()
        TweenService:Create(logo, TweenInfo.new(0.5), {ImageTransparency = 1}):Play()
        TweenService:Create(loadBarBG, TweenInfo.new(0.5), {BackgroundTransparency = 1}):Play()
        TweenService:Create(loadBarFill, TweenInfo.new(0.5), {BackgroundTransparency = 1}):Play()
        TweenService:Create(welcomeText, TweenInfo.new(0.5), {TextTransparency = 1}):Play()
        
        self.MainFrame.Visible = true
        TweenService:Create(self.MainFrame, TweenInfo.new(0.7, Enum.EasingStyle.Back), {Position = UDim2.new(0.5, -275, 0.5, -200)}):Play()
        task.wait(0.7)
        startupFrame:Destroy()
    end)

    -- Toggle UI Keybind (RightControl)
    UIS.InputBegan:Connect(function(input, processed)
        if not processed and input.KeyCode == Enum.KeyCode.RightControl then
            self.MainFrame.Visible = not self.MainFrame.Visible
        end
    end)

    return self
end

function Library:CreateTab(name)
    local tabBtn = Instance.new("TextButton")
    tabBtn.Size = UDim2.new(1, -10, 0, 32)
    tabBtn.Position = UDim2.new(0, 5, 0, 0)
    tabBtn.BackgroundTransparency = 1
    tabBtn.Text = "    " .. name
    tabBtn.TextColor3 = Color3.fromRGB(180, 180, 180)
    tabBtn.Font = Enum.Font.GothamMedium
    tabBtn.TextSize = 12
    tabBtn.TextXAlignment = Enum.TextXAlignment.Left
    tabBtn.Parent = self.TabList
    Instance.new("UICorner", tabBtn).CornerRadius = UDim.new(0, 6)

    local page = Instance.new("ScrollingFrame")
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.Visible = false
    page.ScrollBarThickness = 0
    page.CanvasSize = UDim2.new(0, 0, 0, 0)
    page.AutomaticCanvasSize = Enum.AutomaticSize.Y
    page.Parent = self.PageContainer
    Instance.new("UIListLayout", page).Padding = UDim.new(0, 8)

    tabBtn.MouseButton1Click:Connect(function()
        for _, p in pairs(self.PageContainer:GetChildren()) do p.Visible = false end
        page.Visible = true
    end)
    if #self.TabList:GetChildren() == 1 then page.Visible = true end

    local tabMethods = {}

    -- [[ UI ELEMENT: LABEL ]] --
    function tabMethods:Label(text)
        local l = Instance.new("TextLabel")
        l.Size = UDim2.new(1, -5, 0, 30)
        l.BackgroundTransparency = 1
        l.Text = text
        l.TextColor3 = Color3.fromRGB(200, 200, 200)
        l.Font = Enum.Font.Gotham
        l.TextSize = 13
        l.TextXAlignment = Enum.TextXAlignment.Left
        l.Parent = page
    end

    -- [[ UI ELEMENT: BUTTON ]] --
    function tabMethods:Button(text, callback)
        local btn = Instance.new("TextButton")
        btn.Size = UDim2.new(1, -5, 0, 35)
        btn.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
        btn.Text = text
        btn.TextColor3 = Color3.fromRGB(230, 230, 230)
        btn.Font = Enum.Font.GothamMedium
        btn.TextSize = 13
        btn.Parent = page
        Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 6)
        
        btn.MouseButton1Click:Connect(function()
            btn.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
            task.wait(0.1)
            btn.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
            pcall(callback)
        end)
    end

    -- [[ UI ELEMENT: SLIDER ]] --
    function tabMethods:Slider(text, min, max, default, callback)
        local sliderFrame = Instance.new("Frame")
        sliderFrame.Size = UDim2.new(1, -5, 0, 50)
        sliderFrame.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
        sliderFrame.BackgroundTransparency = 0.95
        sliderFrame.Parent = page
        Instance.new("UICorner", sliderFrame)
        local label = Instance.new("TextLabel")
        label.Size = UDim2.new(1, -20, 0, 25)
        label.Position = UDim2.new(0, 10, 0, 0)
        label.BackgroundTransparency = 1
        label.Text = text .. " : " .. default
        label.TextColor3 = Color3.fromRGB(200, 200, 200)
        label.Parent = sliderFrame
        local tray = Instance.new("Frame")
        tray.Size = UDim2.new(1, -20, 0, 4)
        tray.Position = UDim2.new(0, 10, 0, 35)
        tray.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
        tray.Parent = sliderFrame
        local fill = Instance.new("Frame")
        fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
        fill.BackgroundColor3 = Color3.fromRGB(0, 122, 255)
        fill.Parent = tray
        local dragging = false
        sliderFrame.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 then dragging = true; _G.ViperDraggingSlider = true end
        end)
        UIS.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 then dragging = false; _G.ViperDraggingSlider = false end
        end)
        UIS.InputChanged:Connect(function(input)
            if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
                local pos = math.clamp((input.Position.X - tray.AbsolutePosition.X) / tray.AbsoluteSize.X, 0, 1)
                local val = math.floor(min + (max - min) * pos)
                fill.Size = UDim2.new(pos, 0, 1, 0)
                label.Text = text .. " : " .. val
                pcall(callback, val)
            end
        end)
    end
    return tabMethods
end

-- [[ EXECUTION ]] --
local Win = Library.new("Apple Lib")

local Tab1 = Win:CreateTab("Main")

-- Radar Script Button
Tab1:Button("Radar Script", function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/DFSC-roblox/Plane-Crazy-Scripts/refs/heads/main/simple-radar"))()
end)

Tab1:Slider("WalkSpeed", 16, 200, 16, function(v)
    LocalPlayer.Character.Humanoid.WalkSpeed = v
end)

local About = Win:CreateTab("About")
About:Label("Library: Apple Lib V3")
About:Label("Executor: " .. (identifyexecutor and identifyexecutor() or "Unknown"))
About:Label("Game: " .. GameName)
